<% @content_heading = @news_item.title %>
<div id="news_full">
<p><%=h @news_item.date.strftime("%x") %></p>
<%=markdown @news_item.description %>

</div>
<div id="tool_bar">
	<% if logged_in? %><%= link_to 'Edit', edit_news_item_path(@news_item) %> | <% end %><%= link_to 'Back', news_items_path %>
</div>